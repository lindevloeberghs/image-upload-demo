<p id="notice"><%= notice %></p>

<p>
  <strong>Image:</strong>
  <% if @item.image? %>
  <%= cl_image_tag @item.image.path, width: 200, height: 300, crop: :fill %>
<% end %>
</p>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
